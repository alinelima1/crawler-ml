const products = require('../src/app.js')

describe('Colocar descrição geral', () => {
    describe('Descrição', () => {
        it('colocar retorno esperado', () => {
            // const response = filtro.filterByStatus();
            // expect(response.length).toBe(2)
        })
    })
})